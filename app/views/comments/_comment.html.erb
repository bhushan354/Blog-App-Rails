<p><%= comment.user.name %> => <%= comment.text %></p>

<%= link_to "Delete Comment", comment_path(comment),
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this comment?' },
              class: "delete-comment-btn" if can?(:destroy, Comment) %>
